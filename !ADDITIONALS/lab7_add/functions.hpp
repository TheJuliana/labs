#pragma once
#include <iostream>
#include <vector>

namespace fn{
    void Read(int &n, std::vector<int> &seq);
    bool Nine(int x);
    bool Doubl(int x);
    void Output(std::vector<int> seq);
}